---

---

<section class="about-section">
	<h2 class="about-title">About Me</h2>
	<div class="about-card">
		<div class="about-ice">
			<div class="about-content">
				I'm a passionate software engineer with a focus on AI. I'm
				currently a 2B Software Engineering student at the
				University of Waterloo. The unknown piques my interest and
				I'm always eager to learn new things. In my free time, I
				keep up with the latest AI research, often reading papers on
				arXiv. Outside of academics, I like cooking, traveling,
				working out at the gym, and playing ultimate frisbee. I'm
				always looking for opportunities to learn and grow, so feel
				free to reach out—I'm happy to connect!
			</div>
		</div>
	</div>
</section>

<style>
	.about-section {
		display: flex;
		flex-direction: column;
		align-items: center;
		padding: 6rem 2rem 2rem 2rem;
		position: relative;
		z-index: 3;
	}

	.about-card {
		position: relative;
		overflow: visible;
		border-radius: 0;
		padding: 0;
		max-width: none;
		width: min(92%, 560px);
		margin: 0 auto;
		z-index: 0;

		transform: perspective(900px) rotateX(0deg) rotateY(0deg);
		transform-style: preserve-3d;
		will-change: transform;
		transition:
			transform 180ms ease,
			filter 200ms ease;
	}

	.about-card::before {
		content: "";
		position: absolute;
		inset: -28px;
		background:
			radial-gradient(
				120% 140% at 50% 50%,
				rgba(90, 180, 255, 0.45) 0%,
				transparent 65%
			),
			radial-gradient(
				90% 90% at 15% 8%,
				rgba(255, 255, 255, 0.65) 0%,
				transparent 55%
			),
			radial-gradient(
				110% 110% at 88% 92%,
				rgba(170, 240, 255, 0.55) 0%,
				transparent 60%
			);
		filter: blur(18px);
		opacity: 0.75;
		z-index: -2;
		pointer-events: none;
	}

	.about-card::after {
		content: "";
		position: absolute;
		inset: -18px;
		background:
			linear-gradient(
				150deg,
				transparent 0 62%,
				rgba(236, 252, 255, 0.95) 62% 76%,
				transparent 76%
			)
			top left / 90px 70px no-repeat,
			linear-gradient(
				230deg,
				transparent 0 55%,
				rgba(200, 240, 255, 0.92) 55% 72%,
				transparent 72%
			)
			top right / 150px 120px no-repeat,
			linear-gradient(
				214deg,
				transparent 0 57%,
				rgba(210, 245, 255, 0.9) 57% 74%,
				transparent 74%
			)
			right center / 70px 55px no-repeat,
			linear-gradient(
				8deg,
				transparent 0 70%,
				rgba(220, 250, 255, 0.9) 70% 86%,
				transparent 86%
			)
			left center / 45px 36px no-repeat,
			linear-gradient(
				318deg,
				transparent 0 56%,
				rgba(210, 245, 255, 0.9) 56% 73%,
				transparent 73%
			)
			bottom left / 145px 120px no-repeat,
			linear-gradient(
				82deg,
				transparent 0 60%,
				rgba(236, 252, 255, 0.95) 60% 76%,
				transparent 76%
			)
			bottom right / 118px 92px no-repeat,
			radial-gradient(
				2px 2px at 28% 22%,
				rgba(255, 255, 255, 0.9),
				transparent 70%
			),
			radial-gradient(
				3px 3px at 72% 68%,
				rgba(170, 230, 255, 0.8),
				transparent 70%
			),
			radial-gradient(
				2px 2px at 18% 70%,
				rgba(255, 255, 255, 0.7),
				transparent 70%
			),
			radial-gradient(
				2px 2px at 85% 32%,
				rgba(190, 240, 255, 0.7),
				transparent 70%
			),
			radial-gradient(
				1px 1px at 12% 18%,
				rgba(255, 255, 255, 0.55),
				transparent 70%
			),
			radial-gradient(
				1.4px 1.4px at 92% 54%,
				rgba(170, 230, 255, 0.45),
				transparent 70%
			),
			radial-gradient(
				1px 1px at 38% 10%,
				rgba(255, 255, 255, 0.4),
				transparent 70%
			),
			radial-gradient(
				1.2px 1.2px at 60% 88%,
				rgba(160, 220, 255, 0.4),
				transparent 70%
			),
			radial-gradient(
				0.9px 0.9px at 6% 78%,
				rgba(255, 255, 255, 0.35),
				transparent 70%
			),
			radial-gradient(
				1px 1px at 70% 22%,
				rgba(170, 230, 255, 0.35),
				transparent 70%
			);
		filter: drop-shadow(0 0 8px rgba(130, 210, 255, 0.75));
		opacity: 0.8;
		z-index: -1;
		pointer-events: none;
	}

	.about-ice {
		--ice-shape: polygon(
			-4% 12%,
			4% 0%,
			16% -1%,
			30% 3%,
			44% -2%,
			58% 2%,
			70% 0%,
			86% 6%,
			102% 9%,
			98% 20%,
			104% 38%,
			96% 55%,
			101% 68%,
			94% 80%,
			103% 92%,
			88% 103%,
			72% 97%,
			60% 102%,
			48% 98%,
			34% 103%,
			18% 96%,
			6% 102%,
			-3% 90%,
			3% 76%,
			-5% 61%,
			5% 45%,
			-2% 30%,
			3% 18%
		);

		position: relative;
		overflow: hidden;
		clip-path: var(--ice-shape);
		border-radius: 0;
		padding: 2.2rem 2.3rem;
		border: 6px solid transparent;
		transform: rotate(-1.2deg);
		background:
			radial-gradient(
				110% 150% at 6% -2%,
				rgba(255, 255, 255, 0.95) 0%,
				rgba(214, 245, 255, 0.75) 22%,
				transparent 58%
			),
			radial-gradient(
				150% 150% at 100% 100%,
				rgba(140, 220, 255, 0.7) 0%,
				rgba(70, 160, 220, 0.35) 38%,
				transparent 62%
			),
			radial-gradient(
				180% 160% at 28% 90%,
				rgba(40, 110, 190, 0.45) 0%,
				transparent 60%
			),
			linear-gradient(
				170deg,
				rgba(190, 240, 255, 0.2) 0%,
				rgba(48, 125, 200, 0.26) 55%,
				rgba(6, 35, 100, 0.34) 100%
			)
			padding-box,
			linear-gradient(
				135deg,
				rgba(250, 255, 255, 0.98),
				rgba(165, 235, 255, 0.92),
				rgba(95, 165, 255, 0.98)
			)
			border-box;
		backdrop-filter: blur(16px) saturate(105%);
		-webkit-backdrop-filter: blur(16px) saturate(105%);
		box-shadow:
			0 18px 70px rgba(0, 0, 0, 0.6),
			0 0 80px rgba(110, 200, 255, 0.4),
			inset 0 0 0 1px rgba(255, 255, 255, 0.85),
			inset 0 14px 26px rgba(255, 255, 255, 0.35),
			inset 0 0 60px rgba(210, 250, 255, 0.62),
			inset 0 -25px 40px rgba(8, 20, 45, 0.45);
		filter: drop-shadow(0 0 10px rgba(200, 250, 255, 0.55));
	}

	.about-ice::before {
		content: "";
		position: absolute;
		inset: 6px;
		background:
			linear-gradient(
				165deg,
				rgba(255, 255, 255, 0.55) 0%,
				transparent 45%
			),
			radial-gradient(
				120% 100% at 12% 8%,
				rgba(255, 255, 255, 0.9) 0%,
				transparent 60%
			),
			radial-gradient(
				140% 140% at 92% 96%,
				rgba(6, 30, 90, 0.4) 0%,
				transparent 70%
			);
		box-shadow:
			inset 0 0 0 1px rgba(255, 255, 255, 0.7),
			inset 0 -12px 26px rgba(6, 20, 45, 0.25);
		opacity: 0.9;
		pointer-events: none;
	}

	.about-ice::after {
		content: "";
		position: absolute;
		inset: 0;
		background:
			radial-gradient(
				180% 120% at 15% 20%,
				rgba(255, 255, 255, 0.35) 0%,
				transparent 55%
			),
			radial-gradient(
				160% 140% at 80% 10%,
				rgba(190, 240, 255, 0.32) 0%,
				transparent 60%
			),
			radial-gradient(
				140% 160% at 35% 85%,
				rgba(120, 200, 255, 0.28) 0%,
				transparent 60%
			),
			linear-gradient(
				115deg,
				transparent 0 38%,
				rgba(255, 255, 255, 0.12) 38% 40%,
				transparent 40% 58%,
				rgba(255, 255, 255, 0.1) 58% 60%,
				transparent 60%
			),
			linear-gradient(
				35deg,
				transparent 0 46%,
				rgba(255, 255, 255, 0.09) 46% 47%,
				transparent 47% 65%,
				rgba(255, 255, 255, 0.08) 65% 66%,
				transparent 66%
			),
			radial-gradient(
				40% 60% at 70% 20%,
				rgba(255, 255, 255, 0.35),
				transparent 70%
			),
			radial-gradient(
				50% 80% at 25% 75%,
				rgba(120, 200, 255, 0.25),
				transparent 75%
			),
			radial-gradient(
				1px 1px at 22% 32%,
				rgba(255, 255, 255, 0.9),
				transparent 70%
			),
			radial-gradient(
				1.5px 1.5px at 68% 58%,
				rgba(170, 240, 255, 0.8),
				transparent 70%
			),
			radial-gradient(
				1px 1px at 40% 70%,
				rgba(255, 255, 255, 0.7),
				transparent 70%
			),
			radial-gradient(
				1px 1px at 12% 55%,
				rgba(255, 255, 255, 0.55),
				transparent 70%
			),
			radial-gradient(
				1.2px 1.2px at 78% 36%,
				rgba(190, 240, 255, 0.5),
				transparent 70%
			),
			radial-gradient(
				0.8px 0.8px at 55% 22%,
				rgba(255, 255, 255, 0.45),
				transparent 70%
			),
			radial-gradient(
				1px 1px at 30% 88%,
				rgba(170, 230, 255, 0.45),
				transparent 70%
			),
			radial-gradient(
				0.9px 0.9px at 88% 76%,
				rgba(255, 255, 255, 0.4),
				transparent 70%
			),
			radial-gradient(
				0.7px 0.7px at 46% 48%,
				rgba(210, 250, 255, 0.4),
				transparent 70%
			);
		mix-blend-mode: screen;
		opacity: 0.6;
		pointer-events: none;
	}

	.about-title {
		text-align: center;
		color: #fff;
		font-family: "Sora", sans-serif;
		font-size: 3.5rem;
		margin-bottom: 2rem;
	}

	.about-content {
		position: relative;
		z-index: 1;
		color: rgba(7, 16, 36, 0.95);
		font-weight: 600;
		text-shadow:
			0 1px 0 rgba(255, 255, 255, 0.55),
			0 2px 6px rgba(3, 8, 18, 0.35);
		font-family: Montserrat, sans-serif;
		font-size: 1.1rem;
		line-height: 1.6;
		text-align: left;
	}

	@media (max-width: 767px) {
		.about-title {
			font-size: 2.5rem;
		}
		.about-content {
			font-size: 1rem;
		}
	}
</style>

<script>
	window.addEventListener("DOMContentLoaded", () => {
		const card = document.querySelector(".about-card");
		if (!card) return;

		const body = document.body;
		const enableCursor = () => body.classList.add("cursor-visible");
		const disableCursor = () => body.classList.remove("cursor-visible");

		card.addEventListener("pointerenter", enableCursor);
		card.addEventListener("pointerleave", disableCursor);
		card.addEventListener("focusin", enableCursor);
		card.addEventListener("focusout", disableCursor);

		const canTilt =
			window.matchMedia("(hover: hover) and (pointer: fine)").matches;
		const reduceMotion =
			window.matchMedia("(prefers-reduced-motion: reduce)").matches;
		if (!canTilt || reduceMotion) return;

		const maxTilt = 6;
		const maxTranslate = 8;
		let rafId = null;
		let lastEvent = null;

		const updateTransform = () => {
			rafId = null;
			if (!lastEvent) return;

			const rect = card.getBoundingClientRect();
			const x = (lastEvent.clientX - rect.left) / rect.width;
			const y = (lastEvent.clientY - rect.top) / rect.height;

			const rotateY = (x - 0.5) * maxTilt * 2;
			const rotateX = (0.5 - y) * maxTilt * 2;
			const translateX = (x - 0.5) * maxTranslate * 2;
			const translateY = (y - 0.5) * maxTranslate * 2;

			card.style.transform =
				`perspective(900px) rotateX(${rotateX}deg) ` +
				`rotateY(${rotateY}deg) translate3d(${translateX}px, ${translateY}px, 0)`;
		};

		const onMove = (event) => {
			lastEvent = event;
			if (rafId === null) {
				rafId = requestAnimationFrame(updateTransform);
			}
		};

		const resetTilt = () => {
			lastEvent = null;
			if (rafId !== null) {
				cancelAnimationFrame(rafId);
				rafId = null;
			}
			card.style.transform =
				"perspective(900px) rotateX(0deg) rotateY(0deg) translate3d(0, 0, 0)";
		};

		card.addEventListener("pointermove", onMove);
		card.addEventListener("pointerleave", resetTilt);
		card.addEventListener("blur", resetTilt);
	});
</script>
